Você é um engenheiro sênior fazendo uma auditoria de causa raiz no sistema de execução do Scalpyn.

Preciso que você leia o código e responda com precisão as seguintes perguntas. Não suponha — leia o código real.

## Contexto do problema

ZEC_USDT está aprovado em L3 (capital OK, is_tradable OK, score OK) mas nunca executa. O motivo: `execute_buy_cycle` chama `get_merged_indicators()` que mergeia structural (30m) + microstructure. O `is_complete()` falha porque ZEC não tem linhas 30m no banco de produção. Status gravado: `SKIPPED / QUARANTINED / IndicatorCompleteness`. Timer em L3: 34+ minutos.

## Perguntas que precisam ser respondidas com referência ao arquivo e linha exata

### 1. Uso real do 30m em L3
- O que `execute_buy_cycle` faz com os indicadores 30m depois do merge? Eles entram em algum cálculo de score, signal, ou block condition dentro do fluxo de execução? Ou são carregados mas nunca consumidos ali?
- Existe alguma lógica de execução (entry signal, block rule, position sizing) que depende explicitamente de ADX_30m, RSI_30m ou MACD_30m?

### 2. Contrato de `is_complete()`
- Quais campos exatamente `is_complete()` valida? A lista é fixa no código ou vem do config?
- O gate distingue entre timeframes críticos e timeframes de enriquecimento, ou trata todos como bloqueantes?
- Existe algum nível de "partial completeness" já implementado?

### 3. Origem do merge em L3
- `get_merged_indicators()` foi escrito especificamente para L3, ou é uma função genérica chamada em múltiplos estágios?
- Se genérica: quais outros estágios (L1, L2, scoring, signal evaluation) também a chamam?
- O merge do structural 30m em L3 foi uma decisão explícita documentada em algum comentário/docstring, ou é um efeito colateral do reuso da função?

### 4. Impacto do fix
- Se removermos o structural 30m do contrato de completude em L3, quais outros símbolos além de ZEC seriam desbloqueados agora mesmo?
- Existe algum teste unitário ou de integração que valida o comportamento atual de `is_complete()` em L3? Se sim, ele quebraria com a mudança?

### 5. Alternativas já presentes no código
- Existe alguma flag, parâmetro ou config que já permite executar com indicadores parciais (ex: `allow_partial`, `skip_structural`, `execution_mode`)?
- Existe algum fallback ou default para quando o 30m está ausente?

## Formato da resposta

Para cada pergunta, responda:
- **Resposta direta** (sim/não/valor/comportamento)
- **Arquivo + linha** onde encontrou a evidência
- **Trecho relevante do código** (máximo 10 linhas)
- **Incerteza** (se não encontrou evidência conclusiva, diga explicitamente)

Não proponha soluções ainda. Apenas levante os fatos.