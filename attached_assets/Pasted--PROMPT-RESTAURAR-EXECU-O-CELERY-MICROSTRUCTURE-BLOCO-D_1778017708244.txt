🧠 PROMPT — RESTAURAR EXECUÇÃO CELERY (MICROSTRUCTURE)
🔒 BLOCO DE PROTEÇÃO (OBRIGATÓRIO)

NÃO ALTERAR:

fetch_merged_indicators
indicators_provider
pipeline_scan
evaluate_signals
execute_buy
score_engine
block_engine
estrutura de indicators / ohlcv

NÃO refatorar lógica de trading.
NÃO alterar regras, filtros ou scoring.

🎯 OBJETIVO

Restaurar a execução automática da ingestão OHLCV:

collect_5m → fila microstructure → Celery worker → persistência contínua

Situação atual confirmada:

Coletor funciona via endpoint debug ✔
Banco persiste dados ✔
OHLCV NÃO atualiza automaticamente ❌
Conclusão: Celery não executando microstructure
🧩 ETAPA 1 — VALIDAR CONFIGURAÇÃO DO CELERY

Arquivo:

backend/app/tasks/celery_app.py
Garantir:
1. TASK_ROUTES contém microstructure
TASK_ROUTES = {
    "app.tasks.collect_market_data.collect_5m": {"queue": "microstructure"},
}
2. beat_schedule contém collect_5m
beat_schedule = {
    "collect-5m": {
        "task": "app.tasks.collect_market_data.collect_5m",
        "schedule": 300.0,
    },
}
🧩 ETAPA 2 — GARANTIR WORKER CORRETO

Worker DEVE rodar com:

celery -A app.tasks.celery_app worker \
--loglevel=info \
--queues=microstructure,structural,execution \
--concurrency=2
Validar nos logs:
[INFO] Connected to Redis
[INFO] queues: microstructure, structural, execution
[INFO] Received task: collect_5m
🧩 ETAPA 3 — GARANTIR BEAT ATIVO

Rodar:

celery -A app.tasks.celery_app beat --loglevel=info
Validar:
Scheduler: Sending due task collect-5m
🧩 ETAPA 4 — REDIS (BROKER)

Validar:

REDIS_URL configurado
sem erro Connection refused localhost:6379
🧩 ETAPA 5 — AMBIENTE (CLOUD RUN / REPLIT)

Se Cloud Run:

criar dois serviços/processos:
worker
beat

Se Replit:

criar workflows separados
🧩 ETAPA 6 — VALIDAÇÃO FINAL

Após subir tudo:

SELECT COUNT(*)
FROM ohlcv
WHERE time > NOW() - INTERVAL '5 minutes';
Esperado:
> 0 continuamente
🧩 ETAPA 7 — LOGS ESPERADOS

Worker:

Received task: collect_5m
[PERSIST] success symbol=BTC_USDT

Beat:

Scheduler: Sending due task collect-5m
📊 OUTPUT OBRIGATÓRIO

A IA deve retornar:

1. ROOT CAUSE

ex:

Worker não estava rodando com fila microstructure
2. EVIDÊNCIA
logs do worker
logs do beat
query SQL
3. STATUS
RESOLVIDO / BLOQUEADO
4. PRÓXIMA AÇÃO

objetiva

⚠️ REGRA FINAL

Se após subir worker + beat:

ohlcv continuar 0

→ parar execução
→ reportar erro