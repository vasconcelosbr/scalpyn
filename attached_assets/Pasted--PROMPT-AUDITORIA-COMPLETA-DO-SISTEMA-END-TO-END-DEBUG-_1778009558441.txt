### 🧠 PROMPT — AUDITORIA COMPLETA DO SISTEMA (END-TO-END DEBUG)

---

## 🔒 BLOCO DE PROTEÇÃO (OBRIGATÓRIO)

### ❌ PROIBIDO

* NÃO modificar código
* NÃO refatorar
* NÃO corrigir automaticamente
* NÃO alterar Celery
* NÃO alterar banco

👉 Este prompt é **APENAS INVESTIGAÇÃO**

---

## 🎯 OBJETIVO

Diagnosticar por que:

```text
decision_logs não está sendo alimentado
pipeline_scan não gera dados
trade_tracking está vazio
```

---

## 🧩 ESCOPO COMPLETO

Auditar:

1. Infra (processos)
2. Celery (worker + beat)
3. Pipeline (pipeline_scan)
4. Dados de entrada (indicadores / pools)
5. Banco de dados
6. Fluxo completo end-to-end

---

# 🔍 ETAPA 1 — STATUS DOS PROCESSOS

Verificar:

* [ ] Celery worker está rodando?
* [ ] Celery beat está rodando?
* [ ] Redis (ou broker) está ativo?

---

## Output esperado:

```text
worker: RUNNING / STOPPED
beat: RUNNING / STOPPED
broker: OK / FAIL
```

---

# 🔍 ETAPA 2 — CELERY CONFIG

Verificar:

* [ ] pipeline_scan está registrado no celery_app?
* [ ] está em TASK_ROUTES?
* [ ] está no beat_schedule?
* [ ] qual frequência configurada?

---

# 🔍 ETAPA 3 — EXECUÇÃO REAL DAS TASKS

Buscar logs das últimas execuções:

* pipeline_scan
* decision_log_enricher
* trade_reconciliation
* trade_monitor

---

## Validar:

* [ ] última execução ocorreu?
* [ ] houve erro?
* [ ] exceções estão sendo capturadas?

---

# 🔍 ETAPA 4 — TESTE MANUAL DO PIPELINE

Executar:

```python
pipeline_scan.run()
```

---

## Validar:

* [ ] executa sem erro?
* [ ] gera decision_logs?
* [ ] retorna vazio?

---

# 🔍 ETAPA 5 — DADOS DE ENTRADA (CRÍTICO)

## 5.1 Indicadores

```sql
SELECT COUNT(*) FROM indicators;
```

* [ ] tabela possui dados?
* [ ] dados recentes?

---

## 5.2 Pool de ativos

* [ ] existem símbolos sendo processados?
* [ ] lista está vazia?

---

## 5.3 fetch_merged_indicators

Verificar:

* [ ] retorna dados válidos?
* [ ] retorna vazio?
* [ ] retorna erro?

---

# 🔍 ETAPA 6 — FILTROS DO PIPELINE

Verificar:

* [ ] volume mínimo
* [ ] market cap
* [ ] ATR
* [ ] thresholds

👉 validar se não estão bloqueando 100% dos ativos

---

# 🔍 ETAPA 7 — BANCO DE DADOS

```sql
SELECT MAX(created_at) FROM decision_logs;
```

```sql
SELECT COUNT(*) FROM decision_logs;
```

```sql
SELECT COUNT(*) FROM trade_tracking;
```

---

## Validar:

* [ ] existe escrita recente?
* [ ] existe histórico?

---

# 🔍 ETAPA 8 — FLUXO END-TO-END

Simular fluxo:

```text
pipeline_scan
    ↓
decision_logs
    ↓
enricher
    ↓
trade_tracking
```

---

## Validar cada etapa:

* [ ] pipeline gera decisão?
* [ ] decisão vira trade?
* [ ] trade é monitorado?

---

# 🔍 ETAPA 9 — AMBIENTE

Verificar:

* [ ] backend está apontando para o banco correto?
* [ ] existe múltiplo ambiente (dev/prod)?
* [ ] Vercel vs worker usando DB diferente?

---

# 🔍 ETAPA 10 — ERROS SILENCIOSOS

Buscar:

* try/except que engole erro
* logs incompletos
* returns vazios sem log

---

# 🔍 ETAPA 11 — VALIDAÇÃO FINAL

Responder:

---

## 🧾 OUTPUT OBRIGATÓRIO

### 1. ROOT CAUSE (1 linha)

```text
Ex: "Celery beat não está rodando"
```

---

### 2. EVIDÊNCIA

* logs
* queries
* prints

---

### 3. IMPACTO

```text
pipeline parado → decision_logs vazio → sistema sem dados
```

---

### 4. CLASSIFICAÇÃO

```text
CRÍTICO / MÉDIO / BAIXO
```

---

### 5. AÇÃO NECESSÁRIA (SEM IMPLEMENTAR)

Descrever exatamente o que precisa ser feito

---

### 6. STATUS FINAL

```text
READY
ou
NOT READY
```

---

# ⚠️ REGRA FINAL

Se não houver execução de pipeline:

```text
→ classificar como CRÍTICO
→ sistema NÃO está operacional
```

---
