### 🧠 PROMPT — CORREÇÃO CRÍTICA: INGESTÃO OHLCV (MICROSTRUCTURE)

---

## 🔒 BLOCO DE PROTEÇÃO DE ARQUITETURA (OBRIGATÓRIO)

**NÃO ALTERAR EM HIPÓTESE ALGUMA:**

* fetch_merged_indicators
* indicators_provider
* pipeline_scan
* evaluate_signals
* execute_buy
* score_engine
* block_engine

**NÃO:**

* alterar estrutura da tabela indicators
* acessar indicators_json diretamente
* mudar filas existentes
* refatorar pipeline

---

## 🎯 OBJETIVO

Resolver:

```text
OHLCV = 0 (sem dados recentes)
pipeline sem entrada
decision_logs vazio
```

---

## ⚠️ REGRA PRINCIPAL

A IA deve:

✔ Diagnosticar
✔ Criar mecanismo de execução manual
✔ Validar ingestão
✔ Corrigir execução automática

❌ NÃO alterar lógica de trading
❌ NÃO refatorar sistema existente

---

# 🧩 ETAPA 1 — CRIAR ENDPOINT DE DEBUG (OBRIGATÓRIO)

Criar novo endpoint isolado:

```python
GET /debug/run-collect
```

### Implementação:

* Importar função existente de coleta (ex: run_collect_pipeline ou equivalente)
* Executar dentro do endpoint
* Adicionar logs estruturados:

  * início da execução
  * quantidade de símbolos processados
  * erros capturados

### Exemplo esperado:

```python
@app.get("/debug/run-collect")
def debug_run_collect():
    logger.info("DEBUG COLLECT START")

    result = run_collect_pipeline()

    logger.info(f"DEBUG COLLECT RESULT: {result}")

    return {
        "status": "ok",
        "result": result
    }
```

---

# 🧩 ETAPA 2 — GARANTIR QUE A FUNÇÃO DE COLETA FUNCIONE

Validar dentro da função:

* Está lendo pool_coins com is_approved = true
* Está iterando símbolos corretamente
* Está chamando API da corretora
* Está persistindo em ohlcv

---

## 🔍 Adicionar logs internos:

```python
logger.info(f"Fetching candles for {symbol}")
logger.info(f"Received {len(candles)} candles")
logger.info("Saving OHLCV...")
```

---

# 🧩 ETAPA 3 — VALIDAR INSERÇÃO NO BANCO

Após execução do endpoint, a IA deve instruir rodar:

```sql
SELECT COUNT(*)
FROM ohlcv
WHERE time > NOW() - INTERVAL '1 minute';
```

---

# 🧩 ETAPA 4 — DIAGNÓSTICO AUTOMÁTICO

A IA deve detectar:

---

## CASO A — INSERIU DADOS ✔

```text
Problema = Celery / fila microstructure não executando
```

### Ação:

* Verificar se task está registrada
* Verificar se está no beat_schedule
* Verificar routing para fila microstructure
* Garantir worker escutando:

```bash
--queues=microstructure,structural,execution
```

---

## CASO B — NÃO INSERIU ❌

```text
Problema = coletor
```

### A IA deve:

* validar chamada da API da corretora
* validar retorno de candles
* validar symbol format (ex: BTC_USDT vs BTC/USDT)
* validar tratamento de erro

---

# 🧩 ETAPA 5 — LOGS OBRIGATÓRIOS

Adicionar logs claros para:

* início da coleta
* símbolo processado
* erro por símbolo
* sucesso de persistência

---

# 🧩 ETAPA 6 — NÃO QUEBRAR SISTEMA

* Código deve ser isolado
* Não alterar funções existentes
* Apenas adicionar endpoint e logs

---

# 🧩 ETAPA 7 — OUTPUT FINAL

A IA deve retornar:

---

## 1. ROOT CAUSE

```text
ex: worker não consome microstructure
ou
API não retorna candles
```

---

## 2. EVIDÊNCIA

* logs
* queries

---

## 3. STATUS

```text
RESOLVIDO
ou
BLOQUEADO
```

---

## 4. PRÓXIMA AÇÃO

objetiva e direta

---

# ⚠️ REGRA FINAL

Se após execução:

```text
ohlcv continuar 0
```

→ classificar como CRÍTICO
→ sistema NÃO operacional

---
