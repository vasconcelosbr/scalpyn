Você é um engenheiro sênior trabalhando no Scalpyn.
Regras absolutas: ADDITIVE ONLY · ZERO HARDCODE · leia antes de editar.

Pré-condições desta fase:
  - app/core/trace_context.py já existe
  - tabela trade_decisions já existe (migration aplicada)
  - app/services/decision_audit_service.py já existe

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TAREFA 1 — Instrumentar evaluate_signals.py
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Leia backend/app/tasks/evaluate_signals.py completo.
Faça APENAS as seguintes adições (sem remover nada):

  a) No início de _evaluate_async, para cada usuário ativo, chame
     new_trace() e set_ctx(TraceContext(user_id=..., market_type="spot"))

  b) Antes de cada chamada a SignalEngine.evaluate(symbol), atualize
     o contexto: bind symbol e pool_id no TraceContext atual

  c) Após SignalEngine.evaluate retornar, adicione:
     await record_decision(
         db=db, trace_id=get_trace(), user_id=..., pool_id=...,
         symbol=symbol, market_type="spot", exchange="gate",
         status="REJECTED" se failed_required else "APPROVED",
         stage="L3",
         reason=,
         blocking_rule=,
         rule_details=,
         rules_matched=matched,
         rules_failed=failed_required,
         rules_skipped=skipped,
         indicators_snapshot=indicators,
         latency_ms={"l3": },
     )

  d) Para os gates de SKIP (NOT_TRADABLE, NOT_IN_L3, NO_L3_CHAIN,
     QUARANTINED), adicione chamada ao record_decision com
     status="SKIPPED" e reason=

  e) Adicione extra=get_log_extra() em TODOS os logger.info/warning/error
     existentes dentro do loop de símbolos (não altere o texto das mensagens)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TAREFA 2 — Instrumentar execution_engine.py
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Leia backend/app/services/execution_engine.py completo.
Faça APENAS as seguintes adições:

  a) Em _send_order_to_exchange, ANTES do POST:
     logger.debug(
         "order_payload",
         extra={
             **get_log_extra(),
             "endpoint": "/spot/orders",
             "payload": {
                 "symbol": symbol, "side": side,
                 "order_type": order_type,
                 "quantity": quantity, "price": price
             }
         }
     )

  b) Em _send_order_to_exchange, APÓS receber a resposta:
     logger.debug(
         "exchange_response",
         extra={
             **get_log_extra(),
             "status_code": r.status_code,
             "order_id": data.get("id"),
             "response_body": data,
             "latency_ms": round((time.monotonic() - t_send) * 1000)
         }
     )
     (adicione t_send = time.monotonic() antes do POST para medir latência)

  c) No bloco except genérico de execute_trade, substitua o logger.exception
     existente por:
     logger.exception(
         "execute_trade_failed",
         extra={
             **get_log_extra(),
             "error_type": type(e).__name__,
             "symbol": symbol,
             "side": side,
         }
     )
     (mantenha o raise/comportamento atual, só enriquece o log)

  d) Após db.add(trade) e db.commit() com sucesso, chame:
     await record_decision(
         db=db, trace_id=get_trace(), ...,
         status="APPROVED", stage="EXECUTION",
         trade_id=str(trade.id),
         rule_details={"order_id": exchange_order_id, "side": side, "qty": quantity}
     )

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TAREFA 3 — Endpoint de diagnóstico (NOVO)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Crie app/api/trade_diagnostics.py com:

  GET /api/diagnostics/decisions
    params: symbol?, status?, user_id?, limit=50, offset=0
    retorna: lista de trade_decisions ordenada por decided_at DESC

  GET /api/diagnostics/decisions/{trace_id}
    retorna: todas as decisões do mesmo trace_id (timeline completa)

  GET /api/diagnostics/rejections/summary
    params: user_id?, pool_id?, since=24h
    retorna: agrupado por blocking_rule com contagem e símbolos afetados

Registre o router em app/main.py (additive — app.include_router).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
RESTRIÇÕES DESTA FASE:
  - Não altere assinaturas de funções existentes
  - Não altere lógica de decisão (apenas adiciona instrumentação)
  - Se uma linha de logger já usa extra={}, adicione **get_log_extra() ao dict existente
  - Não adicione await record_decision dentro de blocos try/except que não
    sejam o bloco raiz — crie try/except próprio ao redor do record_decision
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━