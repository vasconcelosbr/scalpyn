Leia antes de alterar:
  backend/app/tasks/shadow_trade_monitor.py
  backend/app/services/shadow_trade_service.py
  backend/alembic/versions/054*.py (migration time_to_result)
  Estudar como FIX C3 (_enrich_one_async) foi implementado
  — replicar exatamente o mesmo padrão

CAUSA RAIZ CONFIRMADA:
record_as_simulation é chamado dentro do db.begin() principal.
Quando falha (coluna time_to_result ausente ou qualquer SQL error),
asyncpg aborta a transação, o COMMIT vira ROLLBACK, e
shadow.status='COMPLETED' é desfeito → trade volta a RUNNING.

FIX D1 — Isolar record_as_simulation (padrão idêntico ao C3)

1. Em _advance_shadow, REMOVER as chamadas:
   - await _capture_exit_features(db, shadow)
   - await record_as_simulation(db, shadow)
   (substituir por comentário de referência ao novo fluxo)

2. Criar função _record_simulation_one_async seguindo
   EXATAMENTE o padrão de _enrich_one_async (FIX C3):
   - Abre CeleryAsyncSessionLocal própria
   - Recarrega o shadow por ID
   - Chama _capture_exit_features + record_as_simulation
     dentro de db_sim.begin()
   - Qualquer SQL error aborta só db_sim — nunca a sessão principal

3. Em _monitor_async:
   - Coletar sim_targets (shadow IDs fechados) DENTRO do
     db.begin() principal, ANTES do commit
   - Após o bloco principal fechar (commit garantido),
     rodar _record_simulation_one_async para cada target

   Garantia: shadow.status='COMPLETED' commita primeiro.
   A gravação da simulação é best-effort posterior e idempotente.

FIX D2 — Migration 055 safety net (idempotente)

Criar backend/alembic/versions/055_*.py:
  - ADD COLUMN IF NOT EXISTS time_to_result ... em trade_simulations
  - ADD COLUMN IF NOT EXISTS source ... em trade_simulations
  - Espelhar exatamente o que a migration 054 deveria ter aplicado
  - upgrade() e downgrade() corretos
  - Totalmente idempotente (IF NOT EXISTS)

VALIDAÇÃO PÓS-DEPLOY:
1. Migration 055 aplicada (verificar alembic current)
2. Aguardar 5 minutos após deploy
3. Query:
   SELECT status, COUNT(*), COUNT(completed_at) AS fechados,
          MAX(last_processed_time) AS ultimo_proc
   FROM shadow_trades GROUP BY status;
   → status COMPLETED com fechados > 0
   → HYPE, UNI, ZEC fechados
4. Log do worker: "Shadow monitor: N processed, N completed, 0 errors"

Regras: ADDITIVE ONLY | ZERO HARDCODE | git pull --rebase primeiro
Replicar o padrão C3 que já está provado — não inventar abordagem nova.