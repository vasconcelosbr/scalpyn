# Authenticated UI Evidence

- Vercel deployment `dpl_2VjExXcK42NjSedTyb9YX1rs8oWZ` [command] is `Ready`, target `preview`, project `scalpyn`.
- Protected preview `/intelligence-runs` returned `HTTP/1.1 200 OK` and `X-Matched-Path: /intelligence-runs` through authenticated Vercel CLI access [command].
- Frontend unit tests, typecheck, focused lint and production build passed [command].
- Interactive authenticated browser proof: `NÃO COMPROVADO`. Browser-runtime bootstrap failed before a tab could be controlled; no screenshot or click-path claim is made.
- Production alias was not changed.
